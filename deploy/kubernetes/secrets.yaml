# WARNING: Replace placeholder values before deploying to production.
# In production, use an external secrets manager (e.g., Vault, AWS Secrets Manager,
# Sealed Secrets) instead of storing secrets in this file.
apiVersion: v1
kind: Secret
metadata:
  name: orchestrator-secrets
  namespace: deployment-orchestrator
type: Opaque
stringData:
  DB_USER: "orchestrator"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  REDIS_PASSWORD: ""
  AUTH_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
